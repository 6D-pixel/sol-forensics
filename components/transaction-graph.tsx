import { TransactionGraphProps } from "@/app/types"

export default function TransactionGraph({
  parameters,
  isLoading,
  hasData,
}: TransactionGraphProps) {
  return <div>transaction-grapG</div>
}