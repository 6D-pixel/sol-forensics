export default function processData(predata:any){
    let graphData;
    //filter by types

    //loop all transactions

    //record native

    //add node

    //add edge

    //fetch balance for size
    return graphData;
}